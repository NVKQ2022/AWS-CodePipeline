project:
  name: devops-homework
  regions:
    - ap-southeast-2

  package_lambda: false

tests:
  default-test:
    template: template/main.yaml
    parameters:
      KeyName: my-key
      MyIP: 0.0.0.0/0
    regions:
      - ap-southeast-2

general:
  cleanup: false